---
meta:
  environment: node
  toolchain:
    name: npm
    version: 18
build:
  artifacts:
    '*': /
run:
  scriptName: index.mjs
  persistenceMount: /data
  containerPort: 80
  command: yarn nuxt  build && node /server/index.mjs
